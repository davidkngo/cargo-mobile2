import QtQuick
import QtQuick.Controls

import "."
import "routes.js" as Routes

ApplicationWindow {
    width: 640
    height: 480
    visible: true
    title: qsTr("{{app.name}}")

    StackView {
        id: stackView
        anchors.fill: parent

        Component.onCompleted: Router.initialize(stackView, Routes.tree, Routes.initialRoute)
    }
}
